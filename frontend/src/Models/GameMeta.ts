export type GameMetaGet =  {
    currentlyPlaying: boolean;
    completed: boolean;
    rating: number;
    notes: string;
};

export type GameMetaUpdate = {
    currentlyPlaying: boolean;
    completed: boolean;
    rating: number;
    notes: string;
}


